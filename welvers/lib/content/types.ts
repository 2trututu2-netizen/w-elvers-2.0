export type ToolContent = {
  slug: string;
  title: string;
  excerpt: string;
  category: string;
  pricing: string;
  affiliateUrl?: string;
  content: string;
};

export type ArticleContent = {
  slug: string;
  title: string;
  excerpt: string;
  content: string;
};
